<ul>
    {% for conference in conferences %}
        <li>
            <a href="{{ path('conference_show', { 'slug': conference.slug }) }}">{{ conference.title }}</a>
        </li>
    {% endfor %}
</ul>
